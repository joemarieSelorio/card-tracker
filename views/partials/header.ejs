<!DOCTYPE html>
<html>
    <head>
        <title>Card Tracker</title>
    </head>
    <link rel="stylesheet" href = "/stylesheets/style.css" >
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <body>
        <nav class="navbar navbar-default">
            <div class="container">
              <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                  <span class="sr-only">Toggle navigation</span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="/main">U-Tracker</a>
              </div>
              <div id="navbar" class="collapse navbar-collapse">
                  <% if(user.username === 'bankUser'){ %>
                    <ul class="nav navbar-nav">
                        <li class=""><a href="/transmittal">Transmittal</a></li>
                    </ul>
                    <ul class="nav navbar-nav">
                        <li><a href="/monitor">Ongoing</a></li>
                        <li><a href="/unverified">Sent Items</a></li>
                        <li><a href="/main">Archives</a></li>
                      </ul>
                    <% }else{ %>
                      <ul class="nav navbar-nav">
                         
                      </ul>
                      <ul class="nav navbar-nav">
                          <li class=""><a href="/received">Inbox</a></li>
                          <li><a href="/received/status">Delivery Status</a></li>
                          <li><a href="/main">Archives</a></li>
                        </ul>
                <% } %>
          
                <% if(!currentUser){ %> 
                  <ul class="nav navbar-nav navbar-right"></ul>
                    <li class="<%= typeof page !== 'undefined' && page === 'login' ? 'active' : '' %>"><a href="/login">Login</a></li>
                    <li class="<%= typeof page !== 'undefined' && page === 'register' ? 'active' : '' %>"><a href="/register">Sign Up</a></li>
                  </ul>
                <% } else {%>
                  <ul class="nav navbar-nav navbar-right">
                      <li><a href="/user/<%= currentUser.id%>">Signed In As <%= currentUser.username %></a></li>
                      <li><a href="/logout">Logout</a></li>
                  </ul>
                <% } %>
              </div>
            </div>
          </nav>
  